# PHPMailer setup

This project includes the PHPMailer SMTP files from the WIS-08 practical:

- `PHPMailer.php`
- `SMTP.php`

Copy `mail_config.php.example` to `mail_config.php`, set `enabled` to
`true`, and enter the shop Gmail address and its app password. Never commit
`mail_config.php`.
